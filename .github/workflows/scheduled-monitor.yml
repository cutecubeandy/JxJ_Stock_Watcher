name: Scheduled JxJ stock monitor

on:
  schedule:
    # 5-minute cadence. Shifted on 2026-09-23 to force GitHub to re-register
    # the scheduled workflow while keeping the same monitoring frequency.
    - cron: "4,9,14,19,24,29,34,39,44,49,54,59 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check-stock:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Show trigger
        run: |
          echo "Triggered by: ${{ github.event_name }}"

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check stock
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          HEARTBEAT_ENABLED: ${{ vars.HEARTBEAT_ENABLED }}
        run: python3 stock_checker.py

      - name: Save stock state when it changes
        run: |
          if ! git diff --quiet -- state.json; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add state.json
            git commit -m "Update stock state"
            git push
          else
            echo "Stock state did not change."
          fi
