name: DISABLED - Scheduled JxJ stock monitor

# Retired on 2026-09-26 after the monitor was migrated successfully to NAS.
# The former schedule has intentionally been removed.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This GitHub workflow has been retired. The active monitor now runs on NAS."
