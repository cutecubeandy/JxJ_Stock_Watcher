name: DISABLED - Scheduled Test

# Retired on 2026-09-26. Preserved only as historical reference.
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
