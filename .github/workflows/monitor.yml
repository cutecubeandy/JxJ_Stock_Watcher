name: DISABLED - Validate JxJ signed stock

# Retired on 2026-09-26 after the monitor was migrated successfully to NAS.
# Kept for reference only. No automatic trigger remains.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This GitHub workflow has been retired. The active monitor now runs on NAS."
